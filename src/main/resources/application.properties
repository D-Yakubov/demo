spring.application.name=demo
spring.datasource.url=jdbc:postgresql://localhost:5432/university_db
spring.datasource.username=staff
spring.datasource.password=db_staff

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

api.security.secret-token=Bearer its-the-secret-token


#This is the test project that's why I didn't include this file in .gitignore file